Node.js
Express
PostgreSQL kullanıldı.

Uygulama dockerize edildi.
Docker komutlarıyla çalıştırılabilir.

backend klasörü içinde;
"docker-compose up --build" komutu kullanılarak çalıştırılabilir.

Bu komutun çalışması için docker & docker-compose bilgisayarınızda kurulu olmalıdır.
